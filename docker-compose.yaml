version: '3'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    command: sleep infinity
    links:
      - backend
    ports: 
      - 3000:3000


  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: sleep infinity
    ports:
      - 5000:5000